# Пример конфигурации веб-сервера
var basePort 8080;
var maxThreads 4;

server {
    name = [[Мой веб-сервер]],
    port = §basePort 100 +§,
    threads = §maxThreads 2 *§,
    endpoints = #( [[/api]] [[/static]] [[/admin]] ),
    enabled = true,
    version = 2.1
}
