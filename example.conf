# Пример конфигурации веб-сервера
var basePort 8080;
var offset 100;

server {
    name = [[Мой веб-сервер]],
    port = §basePort offset +§,
    version = 2.1,
    endpoints = #( [[/api]] [[/static]] [[/admin]] ),
    enabled = true,
    timeout = 30
}
